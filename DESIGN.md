# DESIGN.md — PELENEV.DESIGN

Дизайн-система лендинга **PELENEV.DESIGN** (сайты под ключ для бизнеса и экспертов).
Стек: чистый **HTML + CSS + JS**, без сборщиков. Анимации — **GSAP 3.12 + ScrollTrigger**, плавный скролл — **Lenis**. Шрифт — **Inter/InterVariable** с rsms.me.

> Документ описывает фактическое состояние дизайна. Токены живут в `styles.css` → `:root`.

---

## 1. Арт-дирекшн

Швейцарский минимализм / editorial. Много воздуха, крупная гротескная типографика, контраст **тёмных «кинематографичных»** секций (герой, прайс, футер) и **светлых «галерейных»** (about, услуги, FAQ). Единственный цветовой акцент — **вермилион `#FF3C00`**. Скругления крупные и мягкие. Анимации плавные, «дорогие».

Ритм страницы — чередование тёмных и светлых блоков. Между секциями сохраняется «scroll-card»-переход: входящая секция наезжает со `scale .9 → 1`, уходящая уменьшается `1 → .9`, между ними виден тёмный «шов» (фон `body` = `--ink`).

---

## 2. Цвета (токены)

| Токен | Значение | Назначение |
|---|---|---|
| `--ink` | `#0A0A0A` | текст на светлом, фон тёмных секций, подложка body |
| `--paper` | `#FFFFFF` | фон светлых секций, светлые пилюли |
| `--paper-soft` | `#F4F4F4` | стат-карточки, блок цитаты |
| `--paper-line` | `#EAEAEA` | пунктирные разделители, границы |
| `--muted` | `#8A8A8A` | вторичный текст на светлом |
| `--muted-dark` | `#A6A6A6` | вторичный на тёмном (запас) |
| `--accent` | `#FF3C00` | **вермилион** — точка в лого, активные номера, иконки, точка тумблера |
| `--on-dark` | `#FFFFFF` | текст на тёмном |
| `--on-dark-2` | `rgba(255,255,255,.55)` | вторичный на тёмном |

**Правило акцента:** вермилион — точечно. Точка «.» в PELENEV.DESIGN, номера `0X`, инлайн-иконки, точка активной кнопки прайса, иконки `⊕`, интерактивная волна в футере. Красным **не заливаем крупные плоскости**.

---

## 3. Типографика

Шрифт — `InterVariable` (оптическая ось даёт эффект Inter Display на крупных кеглях), фолбэк `Inter, system-ui`. Без засечек.

| Роль | Токен / размер | Вес | LH | Tracking |
|---|---|---|---|---|
| Гигант «PELENEV.DESIGN» | `clamp(2.4rem, 12vw, 11rem)` | 500 | .8 | -0.04em |
| H2 секций | `--fs-h2` `clamp(3rem, 8vw, 8rem)` | 500 | .95 | -0.02em |
| «Услуги» (компактный H2) | `clamp(2.6rem, 5.5vw, 5rem)` | 500 | .95 | -0.02em |
| Заголовок-параграф About | `--fs-h2-para` `clamp(1.6rem, 3.4vw, 3.2rem)` | 500 | 1.15 | -0.01em |
| Hero-подзаголовок | `clamp(1.9rem, 3.2vw, 3.1rem)` | 500 | 1.1 | -0.02em |
| H3 (проекты/услуги) | `--fs-h3` / `clamp(2rem, 3.5vw, 3.25rem)` | 600 | 1.05 | -0.02em |
| Body | `--fs-body` `1.0625rem` | 400 | 1.5 | 0 |
| Caption / label / номер | `--fs-caption` `.875rem` | 500 | 1.3 | +tracking |

Заголовки — sentence case. Гиганты — заглавными (PELENEV.DESIGN). Составные слова (`веб-дизайн`, `AI-контент`, `вайб-кодинг`) оборачиваются в `.nb { white-space: nowrap }`, предлоги/союзы связываются `&nbsp;`.

---

## 4. Сетка и spacing

- Контейнер: `--container-max: 1440px`, боковые поля `--pad-x: clamp(20px, 4vw, 64px)`.
- Вертикальные отступы секций: `--section-pad-y: clamp(80px, 12vh, 200px)`.
- Радиусы: карточки/секции `--r-card: 24px`, картинки `--r-img: 20px`, статы `--r-stat: 16px`, пилюли `--r-pill: 999px`.
- Пунктирные разделители: `1px dashed var(--paper-line)`.

---

## 5. Компоненты

- **Пилюля (light)** `.pill.pill--light` — белый фон, `--ink` текст, hover `scale(1.02)` + стрелка `+4px`.
- **Пилюля (outline)** `.pill--outline` — контурная на тёмном.
- **Пилюля (dark)** `.pill--dark` — чёрная на светлом.
- **Кнопка со шторкой** `.btn-slide` (hero CTA «Обсудить проект») — справа панель со стрелкой, на hover расширяется на всю кнопку.
- **Тег-пилюля** `.tag` — контурная (About).
- **Стат-карточка** `.stat` — `--paper-soft`, гигантская цифра со счётчиком `0 → target` + суффикс.
- **Тумблер услуги** `.ptoggle` / `.ptoggle__opt` — 3 кнопки, у активной вермилион-рамка + **моргающая точка**, ширина плавно адаптируется.
- **Аккордеон FAQ** `.faq__item` — строка + `⊕`/`⊗`, раскрытие по высоте, открыт один.
- **Волна футера** `#footer-line` — SVG-«звуковая волна», дышит в покое, разгорается у курсора, пружинит обратно.

---

## 6. Карта секций (index.html)

Порядок: **hero → about → services → pricing → faq → footer**. Чат-форма — не секция, а **модалка** (`#contact-modal`), открывается со всех CTA.

1. **Hero** (тёмный) — видео-фон (`assets/video/hero.mp4`, затемнён, увеличен на 1.16 чтобы скрыть вотермарк), слева заголовок+подзаголовок+CTA, справа список услуг, внизу гигант PELENEV.DESIGN (sticky, уезжает вниз при скролле).
2. **About** (светлый, первый после hero — «выглядывает» под шапку) — H2-параграф + body + теги-услуги; справа фото + кнопка «Команда»; ряд из 4 стат-карточек со счётчиками (6+, 500+, 24 ч, 100%).
3. **Services / «Услуги»** (светлый) — 5 строк: номер `0X` + описание + картинки, справа название + кнопка «Смотреть кейсы».
4. **Pricing** (тёмный) — карточка: тумблер Сайт/AI-Контент/Комплекс, цена «от N ₽», кнопка, ряд фактов (Сроки/Подход/Старт); правая белая панель «Что входит в работу» (5 пунктов + цитата).
5. **FAQ** (светлый) — «FAQ» + «Часто задаваемые вопросы» + кнопка; аккордеон из 5 вопросов.
**Чат-форма (модалка `#contact-modal`)** — карточка-мессенджер `.chat`: шапка «Dmitry Pelenev · Online», входящие сообщения, прогрессивная форма из 3 шагов (Сообщение → Имя → Контакт `Telegram/Телефон/Email`) + футер с **обязательным чекбоксом согласия** на политику конфиденциальности и кнопкой отправки. Логика — `initContact` в `main.js`. Открывается со **всех CTA** «обсудить/оставить заявку» (атрибут `data-contact` в index.html; на work.html CTA ведут на `index.html#write` → авто-открытие). Закрытие — крестик/бэкдроп/Esc. Для «Телефон» — выбор страны (`COUNTRIES`, флаг + dial-код) и посимвольная маска `formatPhone` (RU/KZ/BY/UA/US/GB/DE/FR/ES/IT/PL/AE/TR), валидность = точное число цифр по маске. Страна по умолчанию — **по языку сайта** (`document.documentElement.lang`: ru→+7, en→+1 и т.д., `defaultCountryIdx`). В шапке чата — иконка-ссылка `.chat__tg` «Написать в Telegram» (href — плейсхолдер `#`). Отправка **симулируется** (loader → пузырь → «печатает…» → ответ → ✓ Delivered), реальная доставка не подключена. Автосохранение ввода в `localStorage['pelenev_chat']`. Success-галочка зелёная (`--ok`), ошибки оранжевые (`--warn`) — единственные точечные отступления от вермилион-акцента.
6. **Footer** (тёмный) — бегущая строка, меню (Навигация/Соцсети/Напишите в чат), интерактивная волна, гигант PELENEV.DESIGN.

**Вторая страница** `work.html` — только кейсы: тёмный заголовок «Мои кейсы» + **табы-фильтр** (`.wtabs`: Сайты / AI-сайты / AI-контент / Графический дизайн) + sticky-стек проектов с закреплённым фоном + футер. Каждый `.project` помечен `data-cat`; клик по табу в `initProjects` (`applyFilter`) прячет неподходящие (`display:none`), перенумеровывает, пересобирает ScrollTrigger'ы (`clearItems`/`build`) и скроллит к табам. Первый видимый кейс получает `.project--lead` (короткий верхний отступ — секция видна сразу, без «чёрного экрана»). Фон показывается сразу через `setBg`. Категории кейсов — плейсхолдеры под замену; в каждой категории должен остаться ≥1 кейс. Навигация ведёт обратно на `index.html#...`.

---

## 7. Анимации — принципы

- **Скролл:** Lenis (`lerp 0.1`) синхронизирован с `ScrollTrigger.update`.
- **Easing:** `power3.out` для входов, `none`+`scrub` для скролл-скраба.
- **Reveal:** заголовки — построчный/по-словный подъём `y:110%` из-под маски (`.mask`, `.wmask`/`.word`). Картинки — `scale`/`clip-path`.
- **Scroll-card:** каждая секция `scale .9→1` на входе, `1→.9` на выходе.
- **Sticky-стек проектов** (work.html) — закреплённый фон кроссфейдится на фото активного кейса, карточки «дышат» scale.
- **Счётчики / волна / тумблер** — см. компоненты.
- **Reduced motion:** при `prefers-reduced-motion: reduce` скрабы/пульсации/волна замирают, остаются конечные состояния.

---

## 8. Ассеты (`/assets`) — плейсхолдеры под замену

| Файл | Что |
|---|---|
| `video/hero.mp4` | фон героя (desktop, тёкучая абстракция) |
| `video/pricing.mp4` | фон карточки прайса (пока нет — плейсхолдер-градиент) |
| `hero-poster.svg` | постер героя / фон футера |
| `logo.svg` | логотип-«галочка» PELENEV |
| `about-duo.svg` | фото в About |
| `projects/p1…p5.svg` | обложки/фото кейсов |
| `services/s1…s6.svg` | картинки в строках услуг |

Реальные тексты/цены/ссылки — в `index.html`. Технические места помечены комментариями `<!-- заказчик: … -->`.
